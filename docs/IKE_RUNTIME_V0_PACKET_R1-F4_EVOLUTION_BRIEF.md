# IKE Runtime v0 Packet R1-F4 Evolution Brief

## Task ID

- `R1-F4`

## Goal

Extract durable controller-visibility method rules from `R1-F`.

## Required focus

1. What controller-facing runtime visibility rules should now become durable?
2. What still belongs to later phases, not `R1-F`?
3. What duplication risks should stay explicit as future guardrails?

## Required output

- `summary`
- `method_gaps`
- `procedural_memory_candidates`
- `now_to_absorb`
- `future_to_preserve`
- `recommendation`
