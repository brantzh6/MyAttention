# IKE Source Intelligence V1 Active Mainline Transition

Date: 2026-04-13
Status: `active_mainline_transition`

## Purpose

Record the mainline shift that follows `Runtime v0` exit.

## Controller Judgment

`Runtime v0` is now accepted as the first trustworthy operating kernel.

The active project mainline should therefore move to:

- `Source Intelligence V1`

This does not demote Runtime v0 into irrelevance.

It means:

- Runtime v0 becomes accepted substrate
- Source Intelligence V1 becomes the active capability mainline above it

## Why This Shift Is Correct

The most important unresolved product value is no longer runtime-kernel
closure.

It is now:

- source quality
- object/person/source discovery quality
- using the kernel to support one real discovery loop

## Immediate Rule

Do not reopen Runtime v0 by default.

Continue Runtime v0 only for:

1. regressions
2. clearly bounded substrate blockers for the next capability line

Otherwise, move execution energy to Source Intelligence V1.
