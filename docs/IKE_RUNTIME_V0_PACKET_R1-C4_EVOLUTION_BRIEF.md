# IKE Runtime v0 Packet R1-C4 Evolution Brief

## Task ID

- `R1-C4`

## Goal

Extract durable method upgrades from `R1-C` and absorb the reusable parts of
the `R1-B` lifecycle-proof pattern.

## Required focus

1. What runtime-truth rules should now become durable method rules?
2. Which lifecycle-proof rules should become default packet requirements?
3. What still belongs to later phases, not `R1-C`?

## Required output

- `summary`
- `method_gaps`
- `procedural_memory_candidates`
- `now_to_absorb`
- `future_to_preserve`
- `recommendation`
