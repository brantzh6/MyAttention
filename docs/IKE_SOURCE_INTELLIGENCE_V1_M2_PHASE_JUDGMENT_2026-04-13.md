# IKE Source Intelligence V1 - M2 Phase Judgment

Date: 2026-04-13
Phase: `M2 Route-Level Loop Proof Through Existing M1 Path`
Status: `approved_for_bounded_start`

## Purpose

Define the next bounded `Source Intelligence V1` packet after the current M1
inspect/continuity/person-seed slices.

## Why M2 Starts Now

The project already has:

- `M1` inspect-style discovery contract
- source-plan creation + refresh continuity
- versions realism closure
- bounded person-signal seeding closure

The remaining gap is no longer missing fields.

The real gap is that the project still needs one narrow route-level loop proof
that uses the existing M1 code path instead of controller-manual collection.

## M2 Objective

Run one bounded route-level loop through the implemented source-intelligence
path:

1. create or refresh a source plan through the existing discovery-backed path
3. inspect the resulting candidates / plan versions
4. produce one controller-usable evaluation note

## Explicit Non-Goals

- no new source schema family
- no full ranking engine
- no cross-source identity resolution
- no source retirement / lifecycle automation
- no broad UI management console

## Exit Condition

`M2` is complete when the project has:

1. one explicit live-or-test-backed route-level loop proof through the current
   M1 path
2. one evaluation note on result usefulness/noise
3. one explicit next slice decision above that loop
