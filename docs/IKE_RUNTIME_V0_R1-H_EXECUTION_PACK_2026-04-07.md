# IKE Runtime v0 R1-H Execution Pack

## Review Prompt

Review `R1-H` as the next narrow runtime quality-loop phase after materially
complete `R1-G`.

Focus on:

1. whether the phase stays about delegated evidence recovery, not runtime truth
   redesign
2. whether it keeps controller fallback and delegated evidence explicitly
   distinct
3. whether it avoids broadening into new platform surfaces

Be especially critical about:

- fake independent validation
- chat-only results being treated as durable evidence
- process/tooling failures being misreported as runtime semantic failures

## Packet Order

1. `R1-H1` coding
2. `R1-H2` review
3. `R1-H3` testing
4. `R1-H4` evolution

## Current Controller Judgment

- `R1-G` is materially complete with fallback review coverage
- the next preserved gap is independent delegated evidence, not another runtime
  truth semantic hole
- `R1-H` should remain narrow and should not create new runtime objects

## Non-Negotiable Guardrails

- do not alter runtime DB truth semantics here
- do not present fallback evidence as recovered delegated evidence
- do not widen into UI/API/notification/benchmark work
- do not rely on chat-only results
