# IKE Runtime v0 Packet R1-E4 Evolution Brief

## Task ID

- `R1-E4`

## Goal

Extract durable method upgrades from `R1-E` without broadening runtime scope.

## Required focus

1. What project-surface alignment rules should now become durable method rules?
2. Which current-work visibility rules should become default runtime
   requirements?
3. What still belongs to later runtime phases, not `R1-E`?

## Required output

- `summary`
- `method_gaps`
- `procedural_memory_candidates`
- `now_to_absorb`
- `future_to_preserve`
- `recommendation`
