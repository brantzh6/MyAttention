# IKE Runtime v0 - R2-I Phase Judgment

Date: 2026-04-10
Phase: `R2-I First Real Task Lifecycle On Canonical Service`
Status: `candidate next phase`

## Why This Phase Exists

After `R2-H`, the main runtime blocker is no longer service observability or
canonical launch-path ambiguity.

The controller evidence chain now shows:

- reviewed Windows canonical acceptability exists
- promotion readiness is explicit
- the real local canonical `8000` service reaches that reviewed promotable
  shape

That means the next meaningful narrow question is no longer:

- can the service be truthfully inspected?

It becomes:

- can one real runtime task lifecycle execute end-to-end on top of the now
  reviewed canonical service baseline?

## Intended Scope

- exercise one real task lifecycle above the canonical service baseline
- keep the proof narrow, auditable, and bounded
- verify that the task lifecycle uses the real runtime truth path rather than
  provisional-only surface logic

## Explicit Non-Goals

- no broad multi-task orchestration
- no benchmark/runtime merge expansion
- no procedural-memory expansion
- no broad UI/runtime redesign
- no service supervisor redesign

## Entry Condition

This phase should be treated as active only after the controller accepts the
current `R2-H` Windows canonical proof path.

Reference decision brief:

- [D:\code\MyAttention\docs\IKE_RUNTIME_V0_R2-H8_CONTROLLER_DECISION_BRIEF_2026-04-10.md](/D:/code/MyAttention/docs/IKE_RUNTIME_V0_R2-H8_CONTROLLER_DECISION_BRIEF_2026-04-10.md)

## Controller Judgment

`R2-I` is the correct candidate next phase because the next unresolved risk is
no longer launch normalization. It is whether the runtime can close one real
task lifecycle on top of the accepted canonical service path without widening
scope.
